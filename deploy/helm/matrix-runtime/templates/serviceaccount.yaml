{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "matrix-runtime.serviceAccountName" . }}
  labels:
    {{- include "matrix-runtime.labels" . | nindent 4 }}
{{- end }}
